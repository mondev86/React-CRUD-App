{
  "name": "react-crud-root",
  "private": true,
  "scripts": {
    "dev": "npm --prefix modern-react-study run dev",
    "dev:host": "npm --prefix modern-react-study run dev -- --host 127.0.0.1 --port 5174",
    "build": "npm --prefix modern-react-study run build"
  }
}
